# Runtime data (memory DBs, evidence, tokens) - never commit
data/
!training/data/
*.token
*.db-journal
*.db-wal
*.db-shm

# Python artifacts
__pycache__/
*.pyc
.venv/
*.egg-info/

# Type checking and coverage
.mypy_cache/
.coverage
htmlcov/
coverage.xml

# Logs
logs/

# Generated local training artifacts (reproducible via `python3 -m training.train`)
training/checkpoints/
training/lineage.db

# Local-only working files (never commit)
CHECKPOINT.md
mcp_test_workspace/
